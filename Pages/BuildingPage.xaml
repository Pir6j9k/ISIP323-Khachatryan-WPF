<Page x:Class="ISIP323_Khachatryan_WPF.Pages.BuildingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISIP323_Khachatryan_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BuildingPage">

    <Grid Margin="10" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <WrapPanel Grid.Row="0" Margin="5">
                <StackPanel Orientation="Horizontal" Margin="5" >
                    <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="5" Foreground="Blue" FontWeight="Bold" />
                    <Border Margin="5" Padding="5" BorderThickness="2" BorderBrush="DarkBlue" CornerRadius="5"> 
                        <TextBox x:Name="SearchTbx" Width="200" VerticalAlignment="Center" BorderThickness="0" TextChanged="TbxSearch_TextChanged" />
                    </Border>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Производитель:" VerticalAlignment="Center" Margin="5" Foreground="Blue" FontWeight="Bold"/>
                    <Border BorderBrush="DarkBlue" BorderThickness="2" CornerRadius="5" Margin="5" Padding="5">
                        <ComboBox x:Name="CmbManufacturer" Width="150" Background="White" BorderThickness="0" SelectionChanged="CmbManufacturer_SelectionChanged"/>
                    </Border>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Тип данных:" VerticalAlignment="Center" Margin="5" Foreground="Blue" FontWeight="Bold"/>
                    <Border BorderBrush="DarkBlue" BorderThickness="2" CornerRadius="5" Margin="5" Padding="5">
                        <ComboBox x:Name="CmbCategory" Width="150" Background="White" BorderThickness="0" SelectionChanged="CmbCategory_SelectionChanged"/>
                    </Border>
                </StackPanel>
            </WrapPanel>


            <Border Grid.Row="1" BorderBrush="DarkBlue" BorderThickness="2" CornerRadius="5" Margin="5" Padding="5">
                
                <ListView x:Name="LvCatalog" 
                          Grid.Row="1" 
                          HorizontalContentAlignment="Stretch"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden" BorderThickness="0" d:ItemsSource="{d:SampleData ItemCount=5}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="DarkBlue" BorderThickness="1" Background="White" Padding="10" Margin="5" CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Image Source="{Binding image}" Width="64" Height="64" Margin="5" Stretch="Uniform"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="{Binding name}" FontWeight="Bold" Foreground="Blue" FontSize="14" TextWrapping="Wrap" Margin="5"/>
                                        <TextBlock Text="{Binding FullDescription}" Foreground="blue" Margin="5" FontSize="12" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding manufacturer.name}" Foreground="blue" Margin="5" FontSize="10"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding price, StringFormat={}{0:N0} ₽}" FontWeight="Bold" FontSize="14" Foreground="Blue" HorizontalAlignment="Center"/>
                                        <Border Background="Blue"  CornerRadius="5" Padding="5" Margin="5">
                                            <Button Content="Добавить"  Margin="5" Background="Blue" Foreground="White" BorderThickness="0" FontSize="14" FontWeight="Bold" Click="BtnAdd_Click"/>
                                         </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>

        <Border Grid.Column="1" Background="White" BorderBrush="DarkBlue" BorderThickness="2" CornerRadius="5" Margin="5" Padding="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Ваша сборка" FontSize="22" FontWeight="Bold" Margin="10" Foreground="Blue"/>

                <ListView x:Name="LvCart" 
                          Grid.Row="1" BorderThickness="0" 
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Text="{Binding name}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="5" Foreground="Blue" FontSize="14"/>
                                <Border Grid.Column="1" BorderBrush="Red" BorderThickness="2" CornerRadius="5" Margin="5" Background="Red" Height="20" Width="20">
                                    <Button Content="X" Foreground="White" Background="Red" FontSize="10" FontWeight="Bold" BorderThickness="0" Click="BtnRemove_Click" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Grid.Row="2" >
                    <Separator Background="DarkBlue"/>
                    
                    <TextBlock Text="Название сборки:" Foreground="Blue" FontWeight="Bold" FontSize="16" Margin="5"/>
                    <Border BorderBrush="DarkBlue" BorderThickness="1" CornerRadius="5" Margin="5" Padding="5">
                        <TextBox x:Name="TbAssemblyName" BorderThickness="0"/>
                    </Border>
                    
                    <TextBlock Text="Ваше имя (автор):" Foreground="Blue" FontWeight="Bold" FontSize="16" Margin="5"/>
                    <Border BorderBrush="DarkBlue" BorderThickness="1" CornerRadius="5" Margin="5" Padding="5">
                        <TextBox x:Name="TbAuthorName" BorderThickness="0" />
                    </Border>
                    
                    <TextBlock Text="Итого:" Foreground="Blue" FontWeight="Bold" FontSize="16" Margin="5"/>
                    <TextBlock x:Name="TbTotalPrice" Text="0 ₽" FontSize="14" FontWeight="Bold" Foreground="Blue" Margin="5"/>
                    <Border BorderBrush="Blue" BorderThickness="2" CornerRadius="5" Margin="5" Padding="5" Background="Blue" >
                        <Button Content="Сохранить сборку" Foreground="White" Background="Blue" FontSize="14" FontWeight="Bold" BorderThickness="0" Click="BtnSaveAssembly_Click"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
