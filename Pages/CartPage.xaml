<Page x:Class="ISIP323_Khachatryan_WPF.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISIP323_Khachatryan_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CartPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="ProductList" 
                 Grid.Row="1" 
                 d:ItemsSource="{d:SampleData ItemCount=5}" 
                 HorizontalContentAlignment="Center" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" >

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Bisque" BorderThickness="1" Margin="10" Padding="10" Width="200">
                        <StackPanel>
                            <Image Source="{Binding ImagePath}" Height="150"/>

                            <TextBlock Text="{Binding Title}" 
                                       FontWeight="Bold" FontSize="14" TextWrapping="Wrap" Height="40"/>

                            <TextBlock Text="{Binding Price, StringFormat='Цена: {0} ₽'}" 
                                       Foreground="Red" FontSize="16" Margin="0,5"/>

                            <Button Content="Удалить" 
                                    Padding="5" Background="#3498db" Foreground="White" 
                                    Click="Button_Click"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Row="2" Content="Назад" 
            Margin="10" Width="100" Height="30"
            HorizontalAlignment="Left"
            IsEnabled="True"
            x:Name="MainPrevButton" Click="MainPrevButton_Click" />

        <Button Grid.Row="2" Content="Оформить" 
            Margin="10" Width="100" Height="30"
            HorizontalAlignment="Right"
            IsEnabled="True"
            x:Name="MainButton" Click="NextButton_Click"/>
    </Grid>
</Page>
